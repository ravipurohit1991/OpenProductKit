# Normalize line endings to LF so template files render consistently across
# Windows and Linux (matters for the generated typed client, which tooling
# rewrites with LF).
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
