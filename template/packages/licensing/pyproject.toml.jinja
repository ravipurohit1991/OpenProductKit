[project]
name = "{{ pkg_slug }}-licensing"
version = "0.1.0"
description = "Licensing / entitlement abstraction. Ships a dev stub; real providers land in P7."
requires-python = ">={{ python_version }}"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/{{ pkg_slug }}_licensing"]
