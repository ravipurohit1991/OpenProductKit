[project]
name = "{{ pkg_slug }}-ai-runtime"
version = "0.1.0"
description = "Provider-neutral AI generation and metering domain primitives."
requires-python = ">={{ python_version }}"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/{{ pkg_slug }}_ai_runtime"]
