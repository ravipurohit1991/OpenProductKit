# Claude Code guide

Read `AGENTS.md` before making changes — it holds the architecture rule, the
layer map, the command palette, and the recipe for replacing the demo domain
with the real product.

@AGENTS.md
