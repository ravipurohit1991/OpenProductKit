// Values injected by the template generator. This tiny file is the only place
// Jinja touches the frontend, which keeps it clear of JSX brace collisions.
export const APP_NAME = "{{ project_name }}";
export const CLI_NAME = "{{ cli_name }}";
