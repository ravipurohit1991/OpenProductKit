{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The UI talks plain HTTP to the sidecar server, so no IPC permissions are needed beyond core.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
